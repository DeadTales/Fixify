# Qué hace un middleware?

* Manejadores globales de errores para interceptar excepciones y devolver respuestas HTTP estandarizadas.

* Filtros de seguridad (CORS personalizado, limitadores de peticiones/rate limiting).

* Middlewares de auditoría o logging para registrar las peticiones entrantes.